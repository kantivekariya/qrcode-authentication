{
  "name": "qrcode-authentication",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "install-frontend": "cd ./packages/client && npm install",
    "install-backend": "cd ./packages/server && npm install",
    "postinstall": "npm run install-frontend && npm run install-backend",
    "build": "cd ./packages/client && npm run build",
    "start": "cd ./packages/server && npm run dev",
    "dev": "lerna run dev",
    "clean": "lerna clean"
  }
}
